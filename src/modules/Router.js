export const goToForm = () => {
  window.location.replace("/form");
};

export const goToUpload = () => {
  window.location.replace("/upload");
};

export const goToHome = () => {
  window.location.replace("/");
};

export const goToQRCode = () => {
  window.location.replace("/qrcode");
};

export const goToScoreBreakdown = () => {
  window.location.replace("/scorebreakdown");
};
